service:
  name: "test-service"
  version: "2.0.0"

logger:
  level: "DEBUG"
  sampleRate: 0.5
  persistentKeys:
    team: "test-team"

tracer:
  enabled: true
  captureHTTPS: false

metrics:
  namespace: "TestNS"
  captureColdStart: false
